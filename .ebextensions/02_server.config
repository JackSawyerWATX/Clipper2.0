container_commands:
  00_install_server:
    command: "cd /var/app/staging/server && npm install --production"
    ignoreErrors: false
  01_build:
    command: "cd /var/app/staging && npm run build --if-present"
    ignoreErrors: true
