version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci

  build:
    commands:
      - npm run build --if-present

  post_build:
    commands:
      - echo "Build completed"

